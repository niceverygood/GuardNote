import React from "react";
import ReactDOM from "react-dom/client";
import GuardNote from "./GuardNote.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuardNote />
  </React.StrictMode>
);
